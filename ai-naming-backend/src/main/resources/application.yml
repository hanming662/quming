server:
  port: 8082

spring:
  application:
    name: ai-naming
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://116.204.135.42:3306/ai_quming?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root123321
  
  redis:
    host: 116.204.135.42
    port: 6379
    password: root123321
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

# MyBatis Plus
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:/mapper/**/*.xml

# AI Configuration
ai:
  # 支持 OpenAI 兼容格式的API：DeepSeek、硅基流动、阿里云百炼等
  api-key: ${AI_API_KEY:sk-ce2465a5eb6b4bf3b88fb246f4dfb9b3}
  base-url: ${AI_BASE_URL:https://api.deepseek.com}
  model: ${AI_MODEL:deepseek-chat}
  # 流式分片大小（字符数）
  stream-chunk-size: 20

# 小程序配置
wx:
  miniapp:
    appid: ${WX_APPID:wxd2b015771eedba15}
    secret: ${WX_SECRET:54483368b9e997331b4a8e5d62d34a35}

logging:
  level:
    com.aining: debug
